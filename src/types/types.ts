interface navProps {
  title: string;
  link: string;
}

interface clientListProps {
  id: number;
  name: string;
  address: string;
  team: string;
  status_Logo: string;
}

export type { navProps, clientListProps };
